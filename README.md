# WifiSaverApp

A simple Kivy app for Android/Python that saves WiFi SSID and password locally as users check their password strength.

## How to Run

1. Install Kivy: `pip install kivy`
2. Run the app: `python main.py`